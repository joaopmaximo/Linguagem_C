// Fibonnaci

#include <stdio.h>

int main (void) {
    int x, cont = 0, r, aux;

    printf ("Insira um numero inteiro: ");
    scanf ("%d", &x);

    while (cont < x) {
        if 
        aux = 
        cont++;
    }

    return 0;
}